def get_user_details(user_id):
    """Contoh fungsi untuk mendapatkan detail user."""
    # Logika mendapatkan user berdasarkan user_id
    return {"user_id": user_id, "name": "John Doe"}


def update_user_profile(user_id, data):
    """Contoh fungsi untuk memperbarui profil user."""
    # Logika memperbarui profil user
    return {"user_id": user_id, "updated_data": data}
